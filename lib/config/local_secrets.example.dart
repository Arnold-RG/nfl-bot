/// Template for local secrets. Copy to `local_secrets.dart` and fill values.
/// `local_secrets.dart` is gitignored and must never be committed.
class LocalSecrets {
  static const String xaiApiKey = '';
}
